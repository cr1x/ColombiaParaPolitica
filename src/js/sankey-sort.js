import * as d3 from './d3.min';
import { column } from './sankey-draw';

const sortPara = () => {
  // const newColumns = [...column]; // clone array columns

  // // grouped nodes by nGroup (tema)
  // for (const [i] of newColumns.entries()) {
  //   newColumns[i] = Array.from(
  //     d3.group(newColumns[i], (d) => d.nGroup),
  //     ([key, values]) => values
  //   );
  // }
  console.log(column);
};

export { sortPara };
